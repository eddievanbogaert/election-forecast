# Copy to .env.local for local development (never commit .env.local)
# Leave empty to use Vite's proxy (recommended for local dev)
VITE_API_URL=
# Production example:
# VITE_API_URL=https://api-XXXX-uc.a.run.app
